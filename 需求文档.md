# 久坐提醒小工具 — 需求梳理文档

> 版本：v0.1（需求确认稿，开发前对齐用）
> 整理人：逸泽　整理时间：2026-08-21

---

## 1. 项目概述

一款**完全本地运行**的久坐提醒桌面工具。以常驻桌面的轻量浮窗形式，按用户自定义的时间间隔弹出提醒，帮助用户养成定时起身活动的习惯。

### 1.1 目标用户
长时间坐办公位的人群（程序员、项目管理、文案等），需要低打扰、可自定义节奏的久坐提醒。

### 1.2 一句话目标
"不联网、不吃资源、随时看到倒计时、到点弹个气泡框提醒你起来动一动。"

---

## 2. 平台与运行环境（非功能需求）

| 项 | 要求 |
|----|------|
| 支持系统 | Windows / Linux / macOS |
| 资源占用 | 轻量化，运行时内存/CPU 占用极低（Tkinter 单进程，无后台服务、无常驻线程占用） |
| 网络 | **完全本地化，不连接任何网络**（无 telemetry、无更新检查、无外部依赖下载） |
| 依赖 | 仅使用 Python 标准库 + Tkinter（无需第三方 GUI 库即可实现，符合轻量目标） |
| 打包分发 | 各平台分别打包为独立可执行文件（Windows `.exe`、macOS `.app`、Linux 可执行），用户无需安装 Python |

---

## 3. 功能需求

### FR-1 常驻浮窗（倒计时显示）
- 工具启动后在桌面**角落**显示一个半透明小浮窗。
- 浮窗**始终显示**距离下次提醒的剩余时间，**精确到秒**，每秒刷新。
- 浮窗**可鼠标拖动**到任意位置。
- 浮窗样式简洁、不抢焦点、不挡操作（置顶但可点击穿透或低干扰）。

### FR-2 自定义提醒间隔
- 用户可在设置中填写提醒间隔，**单位为分钟**，数值由用户自行输入（如 30、45、60）。
- 支持常见示例档位提示（30 分钟 / 1 小时），但最终以用户填写值为准。
- 输入需做合法性校验（正整数、合理上限，如 1–600 分钟）。

### FR-3 到点弹提醒气泡框
- 计时归零时，弹出**漫画式思考气泡框**（气泡/对话框样式，可带一句简短提示语）。
- 气泡框提供「确认」按钮；用户点击确认后：
  - 气泡框关闭；
  - 计时器**重新开始**从完整间隔倒计时。
- 气泡内文案从内置的 **10 条诙谐幽默文案库**中随机选取一条展示，每次提醒不同；**文案不可由用户自定义修改**。文案库见附录 A。

### FR-4 设置持久化
- 用户设置的提醒间隔**自动保存到本地文件**（如 `config.json`，位于用户数据目录或程序同目录）。
- 下次启动时**自动读取**上次的设置，无需重新填写。

### FR-5 计时控制（暂停 / 跳过）
- **暂停**：临时离开（如开会、吃饭）时可暂停计时，恢复后从暂停点继续。
- **跳过**：可手动跳过当前这一轮提醒，立即重新开始计时（等价于"已活动过，重新计"）。
- 暂停状态下浮窗应有明确状态提示（如显示"已暂停"）。

### FR-6 关闭行为与系统托盘
- 浮窗提供关闭入口（X）。点击 X 时弹出选择对话框，让用户二选一：
  - **退出程序**：结束进程。
  - **最小化到托盘**：浮窗隐藏，程序驻留系统托盘（托盘图标），不退出。
- 系统托盘图标支持：单击/双击恢复浮窗、右键菜单「恢复」「退出」。
- 程序需在各平台实现托盘（Windows 托盘区 / macOS 状态栏 / Linux 系统托盘）。

### FR-7 开机自启（用户自定义）
- 设置中提供「开机自动启动」开关，默认关闭。
- 开启时在各平台写入自启项：
  - Windows：写入注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 或启动文件夹。
  - macOS：写入 `~/Library/LaunchAgents` 的 plist。
  - Linux：写入 XDG `autostart` 的 `.desktop`。
- 关闭开关时移除对应自启项。

### FR-8 浮窗形象（妮子）
- 浮窗以龙哥的宠物猫「妮子」卡通形象作为吉祥物/视觉主体。
- 形象定稿：基于妮子照片生成的 **3D 写实 + Q 萌 + 魔性** 风格卡通图（透明背景），文件：`A_3D_rendered_cute_style_of_th_2026-08-21T02-30-13.png`。
- 浮窗中展示该形象 + 实时倒计时；弹出的提醒气泡框也可结合形象呈现。
- 形象资源随程序打包，用户不可更改（与文案一致，均为内置固定素材）。

---

## 4. UI / UX 细节（已确认决策）

| 维度 | 决策 |
|------|------|
| 浮窗形态 | 角落常驻小窗，始终显示倒计时（**非**仅托盘） |
| 浮窗形象 | 以龙哥宠物猫「妮子」的 3D 卡通形象（灰色虎斑、Q 萌魔性）为浮窗吉祥物，定稿图见附录 B |
| 关闭行为 | 点击浮窗 X 弹出选择：**退出程序** / **最小化到托盘**（二选一） |
| 系统托盘 | 支持托盘图标：最小化后驻留托盘，可右键恢复浮窗 / 退出 |
| 提醒方式 | **仅弹框，无声音**（如需声音后续可加开关） |
| 气泡框样式 | 漫画思考气泡框风格，**内置 10 条诙谐文案，每次随机展示，用户不可自定义** |
| 设置保存 | 持久化到本地配置文件 |
| 开机自启 | 设置中提供开关，**由用户自定义**是否开机启动 |
| 暂停/跳过 | 支持 |

---

## 5. 技术实现建议（初稿，供开发参考）

- **语言/框架**：Python 3 + Tkinter（标准库，零额外 GUI 依赖）。
- **计时逻辑**：主线程用 `after()` 做秒级刷新；倒计时基于时间戳计算（避免 `after` 累积误差），即使窗口短暂卡顿也能保持准确。
- **配置存储**：`json` 本地文件，路径优先取操作系统用户配置目录。
- **跨平台注意**：
  - Windows：浮窗 `overrideredirect` + `wm_attributes('-topmost', 1)`；托盘可选。
  - macOS：Tkinter 顶层窗口样式略有差异，需验证置顶/透明。
  - Linux：依赖 Tk + 原生主题，Wayland 下浮窗行为需实测。
- **打包**：PyInstaller 各平台分别打包；目标体积控制在约 10MB 内。需将妮子形象图（`nizi.png`）与配置文件一并打包为资源。
- **形象资源**：定稿图建议重命名为 `nizi.png` 放入项目 `assets/`，随程序读取显示；用户不可更改。

---

## 6. 已确认细节（来自龙哥补充）

1. **关闭行为（已定）**：点击浮窗 X → 弹窗让用户选「退出程序」或「最小化到托盘」，二选一。因此程序需支持系统托盘。
2. **气泡文案（已定）**：内置 10 条诙谐幽默文案（见附录 A），每次提醒**随机展示一条**，**用户不可自定义**。
3. **开机自启（已定）**：设置中提供开关，由用户自定义是否开机启动，默认关闭。

## 7. 仍为默认的小细节（不阻塞，可后续调整）

1. **多显示器**：浮窗默认出现在主屏角落，跨屏记忆位置暂不做。
2. **间隔保护**：下限 1 分钟、上限 600 分钟，防误填。
3. **暂停后恢复**：暂停时停止倒计时，恢复后接着剩余时间继续。
4. **托盘图标样式**：先用系统默认/简单图形，不做精美图标。

---

## 8. 验收标准（开发完成判定）

- [ ] 在 Win / Linux / macOS 任一平台可启动并常驻浮窗。
- [ ] 浮窗显示妮子 3D 卡通形象，与定稿一致。
- [ ] 浮窗实时显示剩余时间，精确到秒，每秒刷新。
- [ ] 用户可输入分钟数设置间隔，重启后设置保留。
- [ ] 到点弹出漫画气泡框，点确认后重新计时。
- [ ] 支持暂停、跳过，状态在浮窗可见。
- [ ] 全程无任何网络请求（可用抓包/断网验证）。
- [ ] 进程内存占用低，无明显常驻高 CPU。
- [ ] 点击浮窗 X 弹出「退出 / 最小化到托盘」选择；最小化后托盘可恢复、可退出。
- [ ] 气泡文案从 10 条中随机展示，每次提醒不同，且用户无法自定义修改。
- [ ] 「开机自启」开关在各平台生效（开启写入、关闭移除自启项）。

---

## 附录 A：内置诙谐提醒文案（10 条，随机展示）

1. 屁股已经和椅子谈恋爱了，去分开它们五分钟吧～
2. 你的脊椎正在默默吐槽：我就没直起来过？
3. 再不动一动，椅子就要长在你身上了哦。
4. 起来！别让血液只在屁股那一块循环。
5. 颈椎发来消息：主人，我也想伸个懒腰。
6. 据说站起来的人，比坐着的人更接近健康。
7. 你的腿：主人，你还记得我有吗？
8. 久坐一时爽，起来才发现——原来腿还是自己的。
9. 给眼睛和屁股都放个假吧，站起来看看远方。
10. 警告：检测到人类已进化成蘑菇，请起身恢复人形。

## 附录 B：浮窗形象定稿（妮子）

- 来源：龙哥宠物猫「妮子」照片，经 ImageGen 图生图生成。
- 风格：**3D 写实渲染 + Q 萌 + 魔性表情**，灰色虎斑、圆脸大眼、坐姿、透明背景。
- 定稿文件：`A_3D_rendered_cute_style_of_th_2026-08-21T02-30-13.png`（位于项目根目录）。
- 开发处理：建议重命名为 `nizi.png`，放入 `assets/`，随程序打包；用户不可更改。

---

_需求已全部确认（含妮子浮窗形象）。进入开发阶段，先搭最小可运行骨架（浮窗 + 倒计时 + 设置 + 弹框 + 托盘 + 随机文案 + 妮子形象），再补暂停/跳过、开机自启与打包。_
